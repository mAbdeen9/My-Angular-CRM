import { FillterCustomerPipe } from './fillter-customer.pipe';

describe('FillterCustomerPipe', () => {
  it('create an instance', () => {
    const pipe = new FillterCustomerPipe();
    expect(pipe).toBeTruthy();
  });
});
